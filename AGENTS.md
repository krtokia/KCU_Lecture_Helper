# Codex 작업 규칙

## 시작
`docs/HANDOFF.md`, `docs/DEVELOPMENT.md`, `docs/RELEASE.md`와 실제 `src/*.user.js`를 먼저 읽는다.
이전 ChatGPT 대화나 File Library를 자동으로 읽을 수 있다고 가정하지 않는다.
코드가 없으면 누락이라고 보고하고 원본을 기억으로 재작성하거나 가짜 완성 코드를 만들지 않는다.
실제 설치본에서 내보낸 코드가 최우선 기준이다. 가능한 문서·환경 점검은 계속 진행한다.
응답은 한국어로 하고 변경 파일, 동작 변화, 수행한 검사, 미검증 사항을 구분한다.

## 변경 범위
- 첫 작업은 개발환경 이관이다. Navigator 기능 확장·재작성·통합은 별도 작업이다.
- Helper는 배속·알림, Navigator는 차시 제어다. 둘을 임의로 합치지 않는다.
- 실제 @name/@namespace/@version/@match/@grant/@connect/@run-at/업데이트 주소를 확인한다.
- POC 3.1의 참조 코드 존재를 사용자 전달·설치·실행 성공과 혼동하지 않는다.
- 로그 정리를 중지 판정 로직 변경으로 확대하지 않는다. 사용자의 의도적 일시정지도 기존 알림 대상이다.
- 배포용 @downloadURL/@updateURL을 임의로 삭제하거나 none으로 바꾸지 않는다.
- Greasy Fork 주소 복구와 GitHub 배포처 전환은 구별하고 diff에 명시한다.
- @version과 런타임 VERSION이 함께 있다면 릴리스 시 일치시킨다. 이관만으로 새 기능/검증 완료를 선언하지 않는다.

## 개인정보와 배포
개인 ntfy topic/토큰, 학번, IP, 세션 키, 쿠키, 전체 플레이어 URL, 원본 RPC/JSON/로그를 공개 코드·문서·fixture·커밋에 넣지 않는다.
원본은 `.local/`에 백업하고 Git에서 제외한다. 이미 추적된 파일은 .gitignore만으로 보호되지 않는다.
공개 전 개인 설정을 GM 저장소/설정 메뉴로 분리한다. 빈 ntfy URL이면 요청하지 않는다.
개인 하드코딩 주소를 공통 기본값/fallback으로 유지하지 않는다.
개발용 로컬 파일 URL이나 private GitHub 토큰을 배포 코드에 넣지 않는다.
명시적 요청 없이 원격 저장소 생성·공개 전환·push·Greasy Fork 게시를 하지 않는다.

## 실행 원칙
- 실제 영상과 사이트 UI를 기준으로 한다. 출석 기록/시청시간 위조 API, progress/currentTime 조작, 미확인 종료 메시지 합성을 추가하지 않는다.
- 사용자의 직접 정지/탐색을 존중한다. 자동화 취소와 영상 일시정지는 구분한다.
- 학번·과목 수·학기·주차 개수를 하드코딩하지 않는다.
- cross-origin iframe DOM에 직접 접근할 수 있다고 가정하지 않는다. 메시지의 origin/source/대상/run 식별자를 확인한다.
- 재생 클릭, 실제 playing, 종료 후보, 출석 Y, 영상 전체 시청 여부를 구분한다.
- 초기 AJAX, iframe 교체, 새로고침, 수동 조작, 중복 실행, 타임아웃을 고려한다.

## 검증과 기록
`docs/TEST_CHECKLIST.md`를 따른다. 문법 검사·모의 테스트·실제 브라우저 검증을 구분한다.
실행하지 않은 테스트의 통과를 주장하지 않는다. 브라우저/로그인 세션 접근이 없으면 필요한 최소 사용자 확인 절차를 안내한다.
작업 종료 시 HANDOFF.md에 실제 결과와 미해결 사항을 갱신한다.
